# Mantecato Architecture

- `frontend/` is the web app: Vite + React SPA.
- `backend/` is the API: FastAPI on port `8100`, plus the CLI and MCP server.
- Do not add new Next.js files or reintroduce `next/*` conventions.
